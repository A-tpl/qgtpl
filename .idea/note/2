# JSON 基本语法完全指南

## 📋 JSON 是什么？
JSON（JavaScript Object Notation）是一种轻量级的数据交换格式，易读易写，常用于Web数据传输和配置文件。

## 🎯 基本规则
1. **数据是键值对**：`"键": 值`
2. **数据用逗号分隔**：`"key1": value1, "key2": value2`
3. **花括号保存对象**：`{ ... }`
4. **方括号保存数组**：`[ ... ]`
5. **字符串必须用双引号**：`"name"`（单引号无效！）

## 🔤 数据类型
| 类型 | 示例 | 说明 |
|------|------|------|
| **字符串** | `"hello"` | 必须双引号包裹 |
| **数字** | `123`, `3.14`, `-5` | 整数或浮点数 |
| **布尔值** | `true`, `false` | 小写，无引号 |
| **null** | `null` | 表示空值，小写 |
| **对象** | `{"key": "value"}` | 花括号包裹的键值对集合 |
| **数组** | `[1, 2, 3]` | 方括号包裹的值列表 |

## 📝 详细语法

### 1. 对象（Object）









## 📌 最佳实践
1. **始终使用双引号**包裹键名
2. **不添加尾随逗号**（JSON标准不允许）
3. **保持一致的缩进**（通常2或4空格）
4. **复杂结构适当换行**，提高可读性
5. **使用有意义的键名**，避免缩写
6. **数值不用引号**，只有字符串用
7. **处理中文时注意编码**，使用UTF-8
8. **验证JSON格式**（可用在线验证工具）

## 🔧 在线工具推荐
- [JSONLint](https://jsonlint.com/) - JSON验证和格式化
- [JSON Formatter](https://jsonformatter.org/) - 格式化和美化
- [JSON Viewer](http://jsonviewer.stack.hu/) - JSON可视化查看

---

**记住：JSON是一种严格格式的数据交换语言，遵循这些规则才能被正确解析！**
